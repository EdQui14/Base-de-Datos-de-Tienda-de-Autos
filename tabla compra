CREATE TABLE compra (
   id_compra int PRIMARY KEY AUTO_INCREMENT not null,
    fecha_compra date not null,
    hora time,
    FOREIGN KEY (id_cliente) REFERENCES clientes (id_cliente), 
    FOREIGN KEY (id_metodo_depago) REFERENCES Metodo_depago (id_metodo_depago)
    );
  
